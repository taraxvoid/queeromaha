{
  "name": "queeromaha.com",
  "version": "1.0.1",
  "description": "queer stuff in omaha",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "bunx @11ty/eleventy --serve",
    "build": "bunx @11ty/eleventy",
    "approve": "netlify dev:exec -- scripts/approve-maker.mjs",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "netlify dev:exec -- drizzle-kit migrate",
    "db:studio": "netlify dev:exec -- drizzle-kit studio",
    "db:seed": "netlify dev:exec -- node scripts/seed-makers.mjs",
    "serve:cms": "bunx decap-server",
    "prepare": "husky",
    "fix": "prettier --write --plugin=prettier-plugin-astro .",
    "lint": "prettier --check --plugin=prettier-plugin-astro ."
  },
  "keywords": [],
  "author": "Tara <tara.vie@proton.me>",
  "license": "MIT",
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "husky": "^9.1.7",
    "prettier-plugin-astro": "^0.14.1"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.2",
    "drizzle-orm": "^0.45.1"
  },
  "volta": {
    "node": "22.22.2"
  }
}
